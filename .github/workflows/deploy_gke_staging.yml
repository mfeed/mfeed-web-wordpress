# This workflow will build a docker container, publish it to Google Container
# Registry, and deploy it to GKE when there is a push to the "main"
# branch.
#
# To configure this workflow:
#
# 1. Enable the following Google Cloud APIs:
#
#    - Artifact Registry (artifactregistry.googleapis.com)
#    - Google Kubernetes Engine (container.googleapis.com)
#    - IAM Credentials API (iamcredentials.googleapis.com)
#
#    You can learn more about enabling APIs at
#    https://support.google.com/googleapi/answer/6158841.
#
# 2. Ensure that your repository contains the necessary configuration for your
#    Google Kubernetes Engine cluster, including deployment.yml,
#    kustomization.yml, service.yml, etc.
#
# 3. Create and configure a Workload Identity Provider for GitHub:
#    https://github.com/google-github-actions/auth#preferred-direct-workload-identity-federation.
#
#    Depending on how you authenticate, you will need to grant an IAM principal
#    permissions on Google Cloud:
#
#    - Artifact Registry Administrator (roles/artifactregistry.admin)
#    - Kubernetes Engine Developer (roles/container.developer)
#
#    You can learn more about setting IAM permissions at
#    https://cloud.google.com/iam/docs/manage-access-other-resources
#
# 5. Change the values in the "env" block to match your values.
name: Mirror and Deploy to GKE (staging)

on:
  push:
    branches: [ "main" ]
    paths:
      - "k8s/**"
      - ".github/workflows/**"

permissions:
  contents: read
  id-token: write

env:
  # ===== GCP/GKE =====
  PROJECT_ID: mfeed-web-staging
  GKE_CLUSTER: cluster-1
  GKE_REGION: asia-northeast1
  WORKLOAD_IDENTITY_PROVIDER: projects/396254140546/locations/global/workloadIdentityPools/gh-pool/providers/gh-provider
  SERVICE_ACCOUNT_EMAIL: gh-gke-deployer@mfeed-web-staging.iam.gserviceaccount.com

  # ===== Artifact Registry =====
  GAR_LOCATION: asia-northeast1
  REPOSITORY: web-apps

  # ===== WordPress image (mirror settings) =====
  WORDPRESS_TAG: php8.3-apache                      # ← latestは避けてタグ固定
  SRC_IMAGE: docker.io/library/wordpress:${{ env.WORDPRESS_TAG }}
  DST_IMAGE: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/wordpress:${{ env.WORDPRESS_TAG }}

  # ===== Kustomize / K8s =====
  KUSTOMIZE_DIR: k8s/overlays/staging               # あなたの構成に合わせて
  NAMESPACE: default
  DEPLOYMENT_NAME: wordpress

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # WIFでGoogleにログイン（必ず service_account を指定）
      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.SERVICE_ACCOUNT_EMAIL }}
          project_id: ${{ env.PROJECT_ID }}

      # GARにログイン
      - name: Docker login to GAR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GAR_LOCATION }}-docker.pkg.dev
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.auth_token }}

      # 公式イメージ → GAR へミラー（Dockerfile不要）
      - name: Mirror official WordPress image to GAR
        run: |
          docker pull "${SRC_IMAGE}"
          docker tag  "${SRC_IMAGE}" "${DST_IMAGE}"
          docker push "${DST_IMAGE}"

          # （任意）digestを出力しておくと再現性が上がります
          docker inspect --format='Pushed: {{index .RepoDigests 0}}' "${DST_IMAGE}" || true

      # Kustomize を取得
      - name: Install kustomize
        run: |
          curl -sL https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv5.4.3/kustomize_v5.4.3_linux_amd64.tar.gz \
          | tar -xz
          sudo mv kustomize /usr/local/bin/kustomize

      # GKE 認証
      - name: Get GKE credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
          cluster_name: ${{ env.GKE_CLUSTER }}
          location: ${{ env.GKE_REGION }}

      # Kustomize の image を GAR のものに差し替え
      # ※ base の kustomization.yaml で `image: wordpress` を使っている前提。
      #   別名なら `kustomize edit set image <その名前>=${DST_IMAGE}` に変更してください。
      - name: Update image in kustomization
        working-directory: ${{ env.KUSTOMIZE_DIR }}
        run: |
          kustomize edit set image wordpress=${DST_IMAGE}
          # （任意・再現性重視）digest固定にする場合は、上の mirror ステップで
          # DIGEST=$(gcloud artifacts docker images describe "${DST_IMAGE}" \
          #   --format='value(image_summary.digest)' --project "${PROJECT_ID}")
          # kustomize edit set image wordpress=${DST_IMAGE%@*}@${DIGEST}

      # デプロイ＆ロールアウト確認
      - name: Deploy to GKE
        run: |
          kubectl -n ${NAMESPACE} apply -k ${KUSTOMIZE_DIR}
          kubectl -n ${NAMESPACE} rollout status deploy/${DEPLOYMENT_NAME} --timeout=5m
          kubectl -n ${NAMESPACE} get svc -o wide
