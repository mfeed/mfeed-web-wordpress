apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: mfeed-wordpress-backendconfig
spec:
  healthCheck:
    requestPath: /healthz
    port: 80
    type: HTTP
  securityPolicy:
    name: allow-internal-only